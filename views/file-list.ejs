<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css"> <!-- Include your CSS file -->
    <title>File List</title>
</head>
<body>
    <div class="container">
        <h1>File List</h1>
        <ul>
            <% files.forEach((file) => { %>
                <li><a href="/view-file?filename=<%= file %>"><%= file %></a></li>
            <% }); %>
        </ul>
    </div>
</body>
</html>